# Local runtime state — never commit (session recaps, iTerm registry, archives, etc.)
state/
usage-data.js
*.log

# Secrets — never commit
.env

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/

# Python
__pycache__/
*.pyc
.venv/
venv/

# commit-approval marker (local only)
.claude_commit_approved
.claude_push_approved
